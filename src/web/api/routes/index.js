import * as userRouter from './user';
import * as collectionRouter from './collection';
import * as linkRouter from './link';
import * as tagRouter from './tag';

export const routes = [userRouter, collectionRouter, linkRouter, tagRouter];
